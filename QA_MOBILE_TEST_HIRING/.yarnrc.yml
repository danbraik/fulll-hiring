# React Native / Metro / Detox require a real node_modules layout
# (Yarn Berry defaults to Plug'n'Play, which breaks them and ESM-only
# packages like @cucumber/pretty-formatter).
nodeLinker: node-modules
